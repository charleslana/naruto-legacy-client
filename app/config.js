const urlClient = 'http://localhost:5500';

const config = {
    mainNotLogged: `${urlClient}/not-logged.html`,
    mainLogged: `${urlClient}/logged.html`,
    home: `${urlClient}/pages/home.html`,
    login: `${urlClient}/pages/login.html`,
    register: `${urlClient}/pages/register.html`
}

export default config;